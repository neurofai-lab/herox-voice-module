# Examples

This folder is reserved for reproducible D4 execution assets.

Recommended additions before submission:

- short WAV files containing example wake words and commands;
- a script or documented command for replaying/transcribing sample audio;
- expected `/voice_command` and `/voice_command_id` outputs;
- terminal logs showing successful startup and command recognition.
