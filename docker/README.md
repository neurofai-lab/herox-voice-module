# Docker notes

Docker is recommended for ARISE reproducibility. The current package can be built natively in a ROS 2/Vulcanexus workspace.

A Dockerfile or docker-compose setup can be added here if the module is submitted with a containerised execution path. The package code itself has not been modified for this repository restructuring.
